<!--
AUTO-GENERATED by `verify-tree --rebuild-index`.
Source of truth: per-thread frontmatter in `project-brain/threads/*/thread.md` and `project-brain/archive/*/thread.md`.
Hand-edits will be overwritten on the next rebuild. To change thread state, edit the relevant `thread.md` directly and re-run the originating skill (or invoke `verify-tree --rebuild-index` manually).
Last rebuild: {{ISO-8601-UTC timestamp — substituted at render time}}
-->

---
id: {{PRIMARY_PROJECT}}-current-state
title: {{PROJECT_TITLE}} — Current State
primary_project: {{PRIMARY_PROJECT}}
kind: snapshot
---

# {{PROJECT_TITLE}} — Current State

At-a-glance snapshot of what's moving right now. Updated by the same skills that touch `thread-index.md`. If the index is the full registry, this is the dashboard — you should be able to stand up from dinner, open this, and re-orient within 30 seconds.

## Active threads

Threads you can pick up and write in right now.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Threads in review

Promotion PRs open. These are blocked on reviewer attention, not yours.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Parked threads

Paused without archiving. Include a short "why" and (if known) an unpark trigger.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Leaves building

Impl-specs with active build threads. Code PR pending.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Recent merges

Last five promotion merges or code landings, newest first.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Notes

Short-lived notes the team wants at the top of attention. Keep terse; move into a thread if it grows.

<!-- sections below are populated by verify-tree --rebuild-index -->
