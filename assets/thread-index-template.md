<!--
AUTO-GENERATED by `verify-tree --rebuild-index`.
Source of truth: per-thread frontmatter in `project-brain/threads/*/thread.md` and `project-brain/archive/*/thread.md`.
Hand-edits will be overwritten on the next rebuild. To change thread state, edit the relevant `thread.md` directly and re-run the originating skill (or invoke `verify-tree --rebuild-index` manually).
Last rebuild: {{ISO-8601-UTC timestamp — substituted at render time}}
-->

---
id: {{PRIMARY_PROJECT}}-thread-index
title: {{PROJECT_TITLE}} — Thread Index
primary_project: {{PRIMARY_PROJECT}}
kind: index
---

# {{PROJECT_TITLE}} — Thread Index

Every thread in this project, past and present. Maintained by `new-thread`, `promote-thread-to-tree`, and `finalize-promotion`. Do not hand-edit unless fixing a validator error — the skills expect this file to be authoritative for each thread's status at a glance.

## Active

Threads with `status: active`, sorted by most recently touched.

<!-- sections below are populated by verify-tree --rebuild-index -->

## In review

Threads with `status: in-review`. Promotion PR open; leaves awaiting merge.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Parked

Threads with `status: parked`. Paused without archiving; resume via `park-thread --unpark`.

<!-- sections below are populated by verify-tree --rebuild-index -->

## Archived

Threads with `status: archived`. Closed out; kept for audit and re-opening.

<!-- sections below are populated by verify-tree --rebuild-index -->
