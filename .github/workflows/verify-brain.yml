name: verify-brain

on:
  push:
    paths:
      - 'project-brain/**'
      - 'thoughts/**'           # retained for rc3 brains mid-migration
      - 'skills/**'
      - 'scripts/**'
      - 'CONVENTIONS.md'
      - '.github/workflows/verify-brain.yml'
  pull_request:
    paths:
      - 'project-brain/**'
      - 'thoughts/**'
      - 'skills/**'
      - 'scripts/**'
      - 'CONVENTIONS.md'

jobs:
  verify:
    name: Verify brain integrity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install PyYAML

      - name: Run verify-tree
        run: |
          if [ -d "project-brain" ]; then
            python3 scripts/verify-tree.py --brain project-brain
          elif [ -d "thoughts" ]; then
            echo "::warning::legacy thoughts/ directory detected; run scripts/migrate-brain-dir.sh to migrate to the rc4 layout."
            python3 scripts/verify-tree.py --brain thoughts
          else
            echo "No project-brain/ or thoughts/ directory found (pack repo, not a brain); skipping brain validation."
          fi

      - name: Run validator unit tests
        run: python3 -m unittest discover -s scripts -p 'test_*.py' -v

  convention-integrity:
    name: Check validator executable
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install PyYAML

      - name: Verify validator is executable
        run: python3 scripts/verify-tree.py --help > /dev/null
